<header class="header" id="header">
  <nav class="nav container">
    <a href="/" class="nav-logo">
      <img src="/assets/img/logo-nike-name.svg" alt="Jacket Store" />
    </a>

    <div class="nav-menu" id="nav-menu">
      <ul class="nav-list">
        <li><a href="/" class="nav-link active-link">Aura Farm</a></li>
        <li><a href="/categories" class="nav-link">Jacket Ki Dukan</a></li>
        <li><a href="/about" class="nav-link">Hum Kaun Hai?</a></li>
        <li><a href="/special" class="nav-link">Special</a></li>
        <li><a href="/contact" class="nav-link">Baat Karne ke liye</a></li>
      </ul>

      <button class="nav-close" id="nav-close" aria-label="Close menu">
        <i class="ri-close-large-line"></i>
      </button>
    </div>

    <div class="nav-actions">
      <button class="nav-shop" aria-label="Shopping cart">
        <i class="ri-shopping-bag-4-fill"></i>
      </button>
      <button class="nav-search" aria-label="Search products">
        <i class="ri-search-line"></i>
      </button>
      <button class="nav-toggle" aria-label="Toggle menu" id="nav-toggle">
        <i class="ri-menu-line"></i>
      </button>
    </div>
  </nav>
</header>

<style>
  .header {
    position: fixed;
    width: 100%;
    top: 0;
    left: 0;
    background: transparent;
    z-index: 100;
  }

  .nav {
    position: relative;
    height: var(--header-height);
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .nav-logo img {
    width: clamp(40px, 10vw, 50px);
  }

  .nav-actions {
    display: flex;
    align-items: center;
  }

  .nav-actions button {
    color: var(--white-color);
    font-size: 1.5rem;
    cursor: pointer;
    min-width: 44px;
    min-height: 44px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  @media (width <= 1150px) {
    .nav-menu {
      position: fixed;
      top: -120%;
      left: 0;
      width: 100%;
      height: 100dvh;
      background: hsla(0, 0%, 0%, 0.1);
      backdrop-filter: blur(16px);
      -webkit-backdrop-filter: blur(16px);
      padding-block: min(6rem, 15vh) 2rem;
      z-index: 100;
      transition: top 0.4s;
      display: flex;
      align-items: center;
      justify-content: center;
    }
  }

  .nav-list {
    text-align: center;
    display: flex;
    flex-direction: column;
    row-gap: 2rem;
  }

  .nav-link {
    position: relative;
    color: var(--white-color);
    font-weight: 600;
    font-size: clamp(0.875rem, 3vw, 1rem);
    padding: 0.5rem 1rem;
    display: inline-block;
  }

  .nav-link::after {
    content: "";
    width: 0;
    height: 2px;
    background: var(--white-color);
    position: absolute;
    left: 0;
    right: 0;
    bottom: -0.5rem;
    margin-inline: auto;
    transition: width 0.4s;
  }

  .nav-link:hover::after {
    width: 100%;
  }

  .nav-close {
    position: absolute;
    top: 1rem;
    right: 1rem;
    color: var(--white-color);
    font-size: 1.5rem;
    cursor: pointer;
    min-width: 44px;
    min-height: 44px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .show-menu {
    top: 0;
  }

  .scroll-header::after {
    content: "";
    position: absolute;
    width: 100%;
    height: 100%;
    background: hsla(0, 0%, 0%, 0.1);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    top: 0;
    left: 0;
    z-index: -1;
  }

  .active-link::after {
    width: 100%;
  }

  @media (width >= 1150px) {
    .nav {
      height: calc(var(--header-height) + 2rem);
    }

    .nav-logo img {
      width: 70px;
    }

    .nav-close,
    .nav-toggle {
      display: none;
    }

    .nav-actions {
      column-gap: 0.5rem;
    }

    .nav-list {
      flex-direction: row;
      align-items: center;
      column-gap: clamp(2rem, 4vw, 4rem);
    }
  }

  @media (width >= 2048px) {
    .header {
      zoom: 1.2;
    }
  }
</style>
